import { Box, Typography } from "@mui/material";
import strings from "../strings";

const Root = () => {
  return (
    <Box>
      <Typography variant="h2">{strings.mui}</Typography>
    </Box>
  );
};

export default Root;
